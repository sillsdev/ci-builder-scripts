#!/bin/bash
# Update sbuild chroots
set -e

PROGRAM_NAME="$(basename "$0")"

. $(dirname "$0")/common.sh
init "$@"

for distribution in $DISTRIBUTIONS_TO_PACKAGE; do
	for arch in $ARCHES_TO_PACKAGE; do
		DISTRIBUTIONS="$distribution" ARCHES="$arch" ./setup.sh --update
	done
done
